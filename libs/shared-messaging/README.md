# shared-messaging

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-messaging` to build the library.

## Running unit tests

Run `nx test shared-messaging` to execute the unit tests via [Jest](https://jestjs.io).
